# En este archivo se añade solo las propiedades para la configuracion de Spring Cloud Config

# Estamos recuperando el puerto 8012 que sustituye al 8002 (declarado en application.properties)
# La configuracion obtenida sustitulle a la de application.properties
spring.application.name=servicios-pedidos

#URL del servidor de la configuracion

spring.cloud.config.uri=http://localhost:8100